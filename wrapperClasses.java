public class wrapperClasses {
    public static void main(String[] args){

//      WRAPPER CLASS : These are classes that "wrap" primitive data types (like int, char) into objects.

        int num = 10;     // primitive type
        Integer numWrapper = new Integer(10);   // Integer is the wrapper class for int


//      provides a way to use primitive data types as reference data types
//      reference data types contains many useful methods
//      Can be used with Collections like (ex. ArrayList, HashMap) can only store objects, not primitives.



//       Primitive                Reference/Wrapper Class
//      -----------              -------------------------
//        boolean                   Boolean
//        int                       Integer
//        char                      Character
//        double                    Double



//        Autoboxing : automatically converts 'primitive types' to 'wrapper classes' (e.g., int to Integer).

//        int num = 10;             // primitive int
//        ArrayList<Integer> list = new ArrayList<>();
//        list.add(num);            // Java automatically converts int to Integer (autoboxing)


//        Unboxing : converts 'wrapper classes' to 'primitive types' (e.g., Integer to int).

//        Integer wrapperClass = new Integer(4);
//        int num = wrapperClass;










    }
}

